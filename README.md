# express-students-managment
task test example

# Express Student Management

Single-page Student Management app using Express (ES module), Mongoose and plain HTML/JS.
Port: 4000

## Setup
1. Copy `.env.example` to `.env` and set `MONGODB_URI`.
2. `npm install`
3. `npm run dev` (or `npm start`)
4. Open http://localhost:4000

This project follows the Student Task Guide provided. See the guide for acceptance criteria. :contentReference[oaicite:4]{index=4}
